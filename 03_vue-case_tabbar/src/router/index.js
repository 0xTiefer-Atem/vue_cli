//0.引入插件
import Vue from "vue"
import VueRouter from "vue-router"

//1. 加载VueRouter插件
Vue.use(VueRouter);

//2. 创建路由对象
const routes = [

];
const router = new VueRouter({
  routes
});

//3. 导出
export default router

//4. 去main.js引入
